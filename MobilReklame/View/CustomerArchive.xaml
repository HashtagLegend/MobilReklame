<Page
    x:Class="MobilReklame.CustomerArchive"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MobilReklame"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d">
    <Page.DataContext>
        <local:ArchiveViewModel/>
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListView HorizontalAlignment="Left" Height="318" Margin="373,235,0,0" VerticalAlignment="Top" Width="1200" ItemsSource="{Binding CustomerArchive}" SelectedItem="{Binding SelectedCustomer, Mode=TwoWay}"/>
        <AppBarButton Icon="Home" Label="" HorizontalAlignment="Right" Margin="0,161,1458,0">
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="Click">
                    <core:NavigateToPageAction TargetPage="MobilReklame.FrontPage"/>
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </AppBarButton>
        <Button Content="Fjern Kunde" HorizontalAlignment="Left" Margin="227,803,0,0" VerticalAlignment="Top" Height="73" Width="235">
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="Click">
                    <core:CallMethodAction MethodName="RemoveCustomer" TargetObject="{Binding Mode=OneWay}"/>
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </Button>


    </Grid>
</Page>

